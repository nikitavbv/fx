[package]
name = "fx-website"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
axum = { version = "0.8.4", default-features = false }
include_dir = "0.7.4"

fx-sdk = { path = "../../fx-sdk" }
